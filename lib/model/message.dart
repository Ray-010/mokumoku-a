class MessageModel {
  String initialMessage;
  String progressMessage;
  String lastMessage;

  MessageModel({required this.initialMessage, required this.progressMessage, required this.lastMessage});
}